# 개발 에이전트 작업 규칙

## 시작과 문서

- 한국어로 소통하고 커밋 제목과 본문도 한국어로 작성한다. 코드 식별자는 영어로 쓴다.
- 작업 시작 시 이 파일과 `docs/PROJECT.md`, `docs/PLAN.md`를 읽고 실제 파일·Git 상태를 확인한다. 제품과 기술 결정은 PROJECT, 작업 순서·증거·다음 할 일은 PLAN에 기록한다.
- Goal 실행을 요청받으면 `docs/START.md`의 실행 계약을 읽는다. 문서 작성 요청이나 단순 질문만으로 Goal을 시작하지 않는다.
- 현재 사용자 지시와 실행 환경의 상위 규칙이 우선한다. 이 문서는 플랫폼 권한을 확대하거나 승인 검토를 우회하지 않는다.

## 작업 방식

1. 현재 체크포인트의 목표, 변경 범위, 필요한 검증을 짧게 설명한다.
2. 가장 작은 독립적인 기능 또는 수정 단위를 구현한다. 기존 사용자 변경은 보존한다.
3. 변경 범위에 필요한 검증을 실행하고, 실패 원인을 수정한다.
4. `docs/PLAN.md`에 완료 조건, 실행한 검증과 결과, 남은 문제를 간결하게 기록한다.
5. 아래 Git 규칙에 따라 커밋·push하고 다음 체크포인트로 진행한다.

- 이름만 있는 화면, 하드코딩한 가짜 랭킹, 연결되지 않은 버튼을 기능 완료로 취급하지 않는다.
- 일상적인 구현 선택은 합의한 범위 안에서 결정한다. 제품 범위·비용·보안 경계가 달라지는 선택은 이유와 대안을 제시한다.
- 기본은 단일 에이전트의 순차 실행이다. 별도 요청 없이 기능마다 구현·리뷰 에이전트와 반복 리뷰 단계를 붙이지 않는다.
- 기능 단위 검증이 통과하면 같은 상태를 반복 검사하지 않는다. 실패·새 변경·새 위험이 있을 때 해당 범위를 다시 확인한다.
- 사용자가 요청한 기능의 필수 작업이 남았다면 임의로 다음 버전으로 넘기거나 완료 조건을 낮추지 않는다.

## 코드 규칙

- 프로젝트는 한 패키지로 유지한다. 실제 반복과 필요가 생길 때 공통 코드를 추출한다.
- TypeScript strict 검사를 사용한다. 외부 데이터는 런타임에 검증하고, 타입 단언이나 `any`로 검증을 대신하지 않는다.
- React는 웹 화면, Phaser는 렌더링·입력·효과, 순수 TypeScript 게임 코어는 시뮬레이션·판정을 맡는다.
- 게임 코어는 DOM, React, Phaser, Cloudflare 런타임에 의존하지 않는다. 시간·입력·난수 시드를 명시적으로 받는다.
- 시뮬레이션은 고정 시간 간격으로 진행한다. 화면 프레임률과 네트워크 전송 주기는 분리한다.
- 맵·코스 생성은 시드와 규칙 버전으로 재현 가능하게 만든다. 제한된 횟수 안에 유효한 결과가 없으면 검증된 기본 맵을 사용한다.
- 멀티 메시지는 입력 요청으로 취급한다. 위치·점수·승패는 서버가 판정한다. 권한은 서버가 확인한 세션과 방 참가 상태로 결정한다.
- 이름은 표시값이다. 이름 또는 클라이언트가 주장하는 플레이어 ID만으로 기존 기록의 소유권을 인정하지 않는다.
- SQL은 매개변수를 바인딩한다. 경기 결과는 중복 제출·재시도로 두 번 반영되지 않게 한다.
- 위치와 애니메이션을 매 프레임 DB에 저장하지 않는다. 기록 보존에 필요한 완료·체크포인트 이벤트만 저장한다.
- 이벤트 리스너, Phaser 인스턴스, WebSocket, 타이머는 화면 이탈·방 종료 시 정리한다.
- 설명 주석은 규칙의 이유와 예외를 한국어로 적는다. 함수와 모듈은 구체적인 이름과 한 가지 책임을 갖게 한다.
- 자체 제작 또는 사용 권한이 확인된 이미지·음원을 사용한다. 외부 에셋은 출처와 라이선스를 기록한다.

## 검증과 완료 증거

- 실행 명령의 기준은 실제 `package.json`이다. 아직 없는 스크립트를 실행했다거나 통과했다고 기록하지 않는다.
- 게임 규칙·생성기는 Vitest로 판정, 고정 시드 재현, 불가능한 맵의 배제와 기본 맵 복귀를 확인한다.
- 세션·결과 저장·입장 제한은 API/방 경계 테스트로 확인한다. 다른 플레이어 기록 덮어쓰기와 중복 결과 반영을 거절해야 한다.
- UI와 게임 흐름은 실제 브라우저에서 확인한다. 멀티는 서로 독립된 2인 및 4인 브라우저 세션으로 시작·동기화·종료를 확인한다.
- 접속 종료·재접속, 5번째 참가자 거절, 재대결은 해당 기능 변경 시 확인한다. 통신 코드의 존재를 실제 대전 증거로 제시하지 않는다.
- 형식만 바뀐 문서는 링크·내용 정합성과 `git diff --check`로 확인한다. 문서 수정만으로 게임 테스트 전체를 반복하지 않는다.
- 로컬 검증, 배포 파일 검증, 실제 공개 URL 검증을 구분한다. 미검증 사항은 완료로 표시하지 않는다.
- 세션/권한 경계, 비밀정보, 데이터 변경은 위험에 맞는 추가 검증을 수행한다. 검사를 끄거나 보안을 약화해 통과시키지 않는다.

## Git: 한국어 커밋과 자동 push

- 이 프로젝트의 사용자는 완료된 개발 단계 및 독립적인 기능·수정마다 한국어 커밋과 GitHub push를 요청했다. 이 범위의 일반 커밋과 push는 매번 재승인을 묻지 않는다.
- 승인된 저장소: `https://github.com/WooRung/lgd-minigame-proj.git`. 기본 대상: `origin/main`.
- 실행 전에 origin의 소유자·저장소가 위 대상과 같은지, 현재 브랜치가 승인된 대상인지 확인한다. 같은 저장소의 HTTPS/SSH 주소는 허용한다. 다르면 임의로 바꾸지 말고 확인한다.
- 검증이 끝난 논리적 작업 단위마다 실제 변경 경로만 명시적으로 stage한다. staged diff를 확인하고 관련 없는 사용자 변경과 비밀정보를 제외한다.
- 커밋 형식: `type: 한국어 변경 요약`. 필요하면 본문에 변경 이유와 핵심 검증 결과를 한국어로 쓴다. type은 `feat`, `fix`, `refactor`, `test`, `docs`, `chore`를 사용한다.
- 예: `feat: 이름 입력 후 게임을 선택할 수 있도록 로비 추가`, `fix: 재접속 시 플레이어가 중복 생성되는 문제 수정`.
- 커밋 성공 후 현재 승인 브랜치를 명시해 push한다. 이 프로젝트의 기본 명령은 `git push origin main`이다. push 후 원격 브랜치가 커밋을 가리키는지 확인한다.
- 빈 커밋, 파일 저장마다의 커밋, 실패한 검증을 숨긴 완료 커밋은 만들지 않는다. PLAN 갱신은 가능한 한 해당 기능 커밋에 포함한다.
- push 실패 시 로컬 커밋을 보존하고 원인과 미반영 커밋을 알린다. 인증·보호 규칙·원격 변경이 원인이면 사용자에게 필요한 조치를 요청하고 독립적인 로컬 작업은 계속한다.
- 브랜치 생성·변경, merge/rebase, force push, reset, amend, 원격 저장소 생성/교체, 보호 규칙 변경은 별도 명시적 요청이 필요하다.
- push가 자동 배포를 유발하는 연결이 생겼다면 배포 승인 범위를 먼저 확인한다. Git 반영 승인을 배포 승인으로 해석하지 않는다.

## 의존성·실행 승인 경계

- 현재 기본 셋업은 문서와 저장소 운영 규칙 작성이다. 앱 초기화·라이브러리 설치는 `docs/START.md`의 실행 프롬프트를 사용자가 제출할 때 시작한다.
- 그 실행에서는 PROJECT의 최소 의존성 목록 최초 설치, 프로젝트 내부의 로컬 설정 작성, 로컬 개발 서버와 테스트, 로컬 전용 D1 초기화를 허용한다. 코드 설치 버전은 당시 호환되는 안정 버전을 확인해 lockfile로 고정한다.
- 추가 의존성, 설치된 의존성의 업그레이드·제거, 다른 기술 스택 도입은 필요성을 설명하고 별도 승인받는다.
- 라이브러리·SDK·CLI·클라우드 API 사용법은 Context7의 `library` → `docs` 순서로 확인한다. 질문당 최대 3회 명령으로 범위를 좁히고, 요금·제한은 공식 문서에서 다시 확인한다. 조회 실패나 정보 충돌은 숨기지 않는다.
- 실제 Cloudflare 배포, 계정·결제 변경, 원격 D1 생성·마이그레이션·데이터 변경, 운영 환경변수/시크릿 설정, CI·자동 배포 연결은 별도 승인 대상이다. 먼저 검토 가능한 코드·SQL·설정·설명서를 준비한다.
- 자격증명, 세션 비밀값, 토큰을 출력·로그·커밋·프롬프트에 넣지 않는다. 실제 비밀값은 사용자가 안전한 설정 경로로 제공한다.

## Goal과 인계

- 활성 Goal이 있으면 현재 범위를 확인해 이어간다. 같은 목표를 중복 생성하거나 임의의 토큰 예산을 설정하지 않는다.
- 성공 조건을 충족하고 검증·필수 Git 반영이 끝났을 때만 완료로 표시한다. 중단/차단 상태 변경은 실행 환경의 Goal 도구 규칙을 따른다.
- 승인이나 외부 접근이 없어 진행하지 못하는 부분은 PLAN에 이유와 필요한 사용자 행동을 남긴다. 완료 표시나 무한 재시도로 숨기지 않는다.
- 종료 보고에는 완료한 범위, 검증 증거, 커밋·push 상태, 남은 작업을 짧게 쓴다.
